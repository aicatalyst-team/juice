apiVersion: v1
kind: Service
metadata:
  name: juice
  namespace: juice
  labels:
    app.kubernetes.io/name: juice
    app.kubernetes.io/managed-by: autopoc
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: juice
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
